plugins {
    id 'java'
    id 'application'
}

dependencies {
    implementation files('../app/libs/jdbm-1.0.jar')
    implementation libs.sudachi
}

application {
    mainClass = 'io.github.kachaya.skk.SudachiDictConverter'
}

jar {
    manifest {
        attributes 'Main-Class': 'io.github.kachaya.skk.SudachiDictConverter'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

tasks.withType(JavaExec) {
    jvmArgs "-Dfile.encoding=UTF-8"
}

tasks.register('runSudachiDictConverter', JavaExec) {
    group = 'application'
    description = 'Runs the SudachiDictConverter tool'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'io.github.kachaya.skk.SudachiDictConverter'
    workingDir = rootProject.projectDir
}

tasks.register('runEmojiConverter', JavaExec) {
    group = 'application'
    description = 'Runs the EmojiConverter tool'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'io.github.kachaya.skk.EmojiConverter'
    workingDir = rootProject.projectDir
}
