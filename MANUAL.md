# ユーザーマニュアル

### 1. モードの切り替え

- **かな・カナ切替**: `q`
  - ひらがなからカタカナ（設定有効時は半角カタカナを含む）へ順次トグルします。
- **英数モード (ASCII)**: `l`
  - 半角英数入力に切り替えます。
- **全角英数モード**: `L`
  - 全角英数入力に切り替えます。
- **Abbrev モード**: `/`
  - アルファベットを直接入力して、英単語から漢字への変換などを行います。
- **英語 / 日本語切替**: `Ctrl-Space`
  - 英語モード（半角英数）と日本語モード（ひらがな）を交互に切り替えます。
- **ひらがなに戻る**: `Ctrl-j`
  - どのモードや状態からでも、ひらがなモード（通常状態）へ戻ります。

### 2. 漢字変換

#### 基本的な流れ

1. **読みの入力 (▽)**: `Shift` キーを押しながら最初の文字を入力してください（例: `Shift-K` `a` `n` `j` `i` → ▽かんじ → `Space` → ▼漢字）。
2. **送り仮名の入力**: 読みの途中で `Shift` キーを押しながら送り仮名の最初の文字を入力してください（例: `Shift-U` `g` `o` `Shift-K` → ▽うご*k → `u` → ▼動く）。
3. **変換の実行**: `Space` キーを押すと第1候補が表示されます（▼）。
4. **候補の選択**: `Space` キーや `→` キー（または `Ctrl-n`, `Ctrl-f`）で次の候補を、`x` キーや `←` キー（または `Ctrl-p`, `Ctrl-b`）で前の候補を選択してください（※ フローティング表示時は `↑` `↓` `←` `→` や `Ctrl-p` / `Ctrl-n` / `Ctrl-b` / `Ctrl-f` により 2 次元上下左右移動・自動スクロールで選択可能です）。
5. **確定**: `Enter` キー、`Ctrl-j`、または次の文字を入力することで選択中の候補を確定してください。

#### 応用的な変換操作

- **再変換**: `Ctrl-u` または `Ctrl-g` (直接入力時)
  - 直前に確定した単語を取り消して未確定状態（変換中）に戻し、やり直します。
- **カーソル位置のひらがな変換**: `Ctrl-l` / `Ctrl-r`
  - `Ctrl-l`: カーソル前のひらがな文字列を自動で抽出して見出し語とし、変換を開始します。
  - `Ctrl-r`: カーソル後のひらがな文字列を自動で抽出して見出し語とし、変換を開始します。
- **接頭辞・接尾辞入力**: `>` / `<` / `?`
  - 見出し語入力の開始時にこれらの記号を入力することで、接頭辞・接尾辞として辞書検索を行います。
- **継続変換（接尾語）**: `>` (変換中)
  - 候補を選択した状態で `>` を入力すると、その候補を確定した直後に接尾語（`>`）を付与した状態で見出し語入力を開始します。
- **見出し語内でのカーソル移動・編集**: `Ctrl-b` / `Ctrl-f` または `←` / `→`
  - 見出し語入力中（▽）にバッファ内の任意の位置へカーソルを移動して文字を挿入・削除（`Ctrl-d` でカーソル直後削除、`Ctrl-k` で行末まで削除、`Ctrl-w` / `Backspace` でカーソル直前削除）したり、`Ctrl-h`（先頭移動）・`Ctrl-e`（末尾移動）を行ってください。
- **送り仮名待ちへの移行**: `Q` (Shift-q, 見出し語入力中)
  - 見出し語入力中（▽）に `Q` (Shift-q) を押すと、送り仮名入力待ち状態へ移行します。
- **動的候補**: `today`, `date`, `tomorrow`, `tdate`, `yesterday`, `ydate`, `now`, `time`, `year`, `month`, `day` のキーワード
  - 見出し語入力中（▽）または Abbrev モード中にこれらのキーワードを入力してスペースキーまたは `>` を押すと、現在の日時や時刻、和暦（令和等）、明日の日付や昨日の日付等を変換候補として表示します。
- **Abbrev モード大文字適合 (Capitalization Auto-Match)**:
  - Abbrev モード（`/`）で英字見出し語を入力する際、入力した見出し語のキャピタライズ形式（小文字 `computer` / 頭大文字 `Computer` / 全大文字 `COMPUTER`）に合わせて、変換結果の英字テキストを自動で整形します。
- **カタカナ確定**: `q` (見出し語入力中)
  - 入力中の読み（▽）を、変換を介さずそのままカタカナとして確定します。

#### 数値変換

辞書に登録された数値テンプレート（#0〜#5）に基づき、入力された数値を以下の形式に変換して表示します。

- **#0**: そのまま (例: 123)
- **#1**: 全角 (例: １２３)
- **#2**: 漢数字 (例: 一二三)
- **#3**: 位取りあり漢数字 (例: 百二十三)
- **#4**: 位取りあり漢数字・旧字体 (例: 壱百弐拾参)
- **#5**: 位取りあり混合表記 (例: 1億2345万6789)

### 3. 入力補完

- **補完の開始・次候補選択**: `Tab` または `Ctrl-i`
  - 見出し語入力中（▽）または Abbrev モード中に、辞書にある単語から補完候補を提示します。繰り返し押すことで候補を順次切り替えます。
- **補完の確定**: `.` (ピリオド) または 補完選択中の `q`
  - 補完候補が選択されている状態でピリオドを押すか、`q` を押すとその補完候補を確定します。

### 4. 個人辞書と学習

- **ユーザー辞書学習の有効/無効**:
  - 設定画面の「変換学習を有効にする」から、変換確定時の単語学習（順位の記憶や新規登録）を制御できます。
- **候補の最後で辞書登録する**:
  - 設定の「候補の最後で辞書登録する」が有効な場合、候補リストの末尾を超えると自動的に単語登録モードへ移行します。無効時は最初の候補に戻ります。
- **候補の削除**: `X` (Shift-x)
  - 変換中に `X` を押すと、現在選択している候補をユーザー辞書から削除できます（ユーザー辞書由来の候補のみ対象）。
- **ユーザー学習辞書ツール**:
  - 学習された単語の一覧表示、個別削除、ファイルへのエクスポート、およびテキストファイルからのインポートを行えます。
- **外部SKK辞書ツール**:
  - 「定番SKK辞書をダウンロード (Web)」から `SKK-JISYO.L`, `SKK-JISYO.M`, `SKK-JISYO.jinmei` 等の公式辞書ファイルを外部ブラウザでダウンロードしてください。
  - ダウンロードした SKK 辞書ファイルを端末ローカルからインポートして検索対象に追加してください（EUC-JP / UTF-8 自動判定および進捗表示に対応）。
  - 学習辞書とは分離管理されているため、クリアや再インポートも個別に行えます。

### 5. 状態別キー操作一覧

#### 通常モード（直接入力）

| キー操作 | 機能 |
|:-|:-|
| `q` | かな・カナ切替（ひらがな→カタカナ→半角カナ） |
| `l` | 英数（ASCII）モードへ切替 |
| `L` | 全角英数モードへ切替 |
| `/` | Abbrevモード（英単語漢字変換）へ切替 |
| `>` / `<` / `?` | 接頭辞・接尾辞トリガーによる見出し語入力開始 |
| `Ctrl-u` | 再変換（直前の確定を取り消して変換し直す） |
| `Ctrl-l` | カーソル前のひらがなを変換 |
| `Ctrl-r` | カーソル後のひらがなを変換 |
| `Ctrl-Space` | 英語 / 日本語モード切替 |
| `Ctrl-s` | 設定画面を開く |
| `Ctrl-j` | ひらがなモードへ復帰 |
| `Ctrl-g` | キャンセル / 再変換 |
| `Ctrl-q` | かな / カナ切替 |
| `Ctrl-d` | カーソル右側の1文字削除 (Forward Delete) |
| `Ctrl-h` | 行頭移動 (Line Start) |
| `Ctrl-e` | 行末移動 (Line End) |
| `Ctrl-k` | カーソル位置から行末まで削除 (Kill Line) |
| `Ctrl-w` | 1文字削除 (Backspace 相当) |

#### 見出し語入力中（▽）

| キー操作 | 機能 |
|:-|:-|
| `Space` | 変換開始 |
| `大文字 (Shift + Key)` | 送り仮名入力の開始（例：`Shift-T` で「▽...*た」） |
| `q` | 現在の読み（または選択中の補完候補）をカタカナで確定 |
| `Q` (Shift-q) | 送り仮名入力待ち状態へ移行 |
| `>` / `<` / `?` | 動的候補の表示 / 接頭辞・接尾辞を伴う変換開始 |
| `Ctrl-b` / `←` (DPAD) | ▽バッファ内でカーソルを左へ移動 |
| `Ctrl-f` / `→` (DPAD) | ▽バッファ内でカーソルを右へ移動 |
| `Ctrl-h` | ▽バッファの先頭へカーソル移動 (Line Start) |
| `Ctrl-e` | ▽バッファの末尾へカーソル移動 (Line End) |
| `Ctrl-d` | カーソル直後の1文字削除 (Forward Delete) |
| `Ctrl-k` | カーソル位置から見出し語末尾まで削除 (Kill Line) |
| `Ctrl-w` / `Backspace` | カーソル直前の1文字削除 |
| `Ctrl-q` | 読みのかな/カナ切替 |
| `Tab` / `Ctrl-i` | 補完の開始・次候補選択 |
| `.` | 選択中の補完候補を確定 |
| `Ctrl-g` | 入力の中断（確定モードへ復帰） |

#### Abbrevモード

| キー操作 | 機能 |
|:-|:-|
| `Space` | 変換開始 |
| `>` | 動的候補の表示 / `>` の追加 |
| `Ctrl-q` | 入力中の英単語を全角英数で確定 |
| `Tab` / `Ctrl-i` | 補完の開始・次候補選択 |
| `.` | 選択中の補完候補を確定 |
| `Ctrl-g` | 入力の中断（確定モードへ復帰） |

#### 変換候補選択中（▼）

| キー操作 | 機能 |
|:-|:-|
| `Space` / `→` (DPAD) | 次の候補へ移動 |
| `x` / `←` (DPAD) | 前の候補へ移動 |
| `>` | 現在の候補を確定し、続けて接尾語（`>`）見出し語入力を開始 |
| `Ctrl-n` / `Ctrl-f` | 次の候補へ移動 |
| `Ctrl-p` / `Ctrl-b` | 前の候補へ移動 |
| `X` (Shift-x) | 現在選択中の候補を個人辞書から削除（ユーザー辞書由来のみ） |
| `Enter` / `Ctrl-j` | 選択中の候補を確定 |
| `Ctrl-g` | 変換の中断（見出し語入力中 ▽ へ復帰） |

#### 全般・共通操作

| キー操作 | 機能 |
|:-|:-|
| `Enter` | 現在の入力を確定 / 改行の挿入 |
| `Backspace` | 1文字削除 / 状態のキャンセル（変換中止など） |
| `Ctrl-j` | どの状態からでも「ひらがなモード」の通常状態へ復帰 |
| `Ctrl-g` | 入力の中断・キャンセル |

#### Ctrlキーショートカットの一覧とカスタマイズ

設定の「Ctrlキーショートカットの設定」画面から、Ctrl + キーの組み合わせ動作の確認およびカスタマイズを行ってください。

システム予約キー（`Ctrl-a`, `Ctrl-c`, `Ctrl-v`, `Ctrl-x`, `Ctrl-y`, `Ctrl-z`）および固定の SKK 予約キー（`Ctrl-j`, `Ctrl-g`, `Ctrl-q`, `Ctrl-f`, `Ctrl-b`, `Ctrl-p`, `Ctrl-n`）を除くすべてのキーについて、以下の機能から選択して割り当てを変更できます：
- `なし (割り当てなし)`
- `左移動 / 前候補`
- `右移動 / 次候補`
- `上移動 / 前候補`
- `下移動 / 次候補`
- `カーソル右削除 (Forward Delete)`
- `行頭移動 (Line Start)`
- `行末移動 (Line End)`
- `行末まで削除 (Kill Line)`
- `行頭まで削除 (Kill Line Backward)`
- `設定画面を開く`
- `確定 / かなキー`
- `キャンセル`
- `かな / カナ切替`
- `英語 / 日本語切替`
- `再変換`
- `カーソル前のひらがなを変換`
- `カーソル後のひらがなを変換`
- `1文字削除`
- `補完`

##### Ctrlキーショートカット一覧表

ショートカットの割り当ては「Ctrlキーショートカットの設定」でカスタマイズできます。

| キー | 変更可否 | 理由 / デフォルト値 | 機能・説明 |
|:---|:---|:---|:---|
| `Ctrl-Space` | **可能** | デフォルト: `英語 / 日本語切替` | 英語モードと日本語モードを切り替え |
| `Ctrl-Enter` | **可能** | デフォルト: `なし (割り当てなし)` | 未割り当て |
| `Ctrl-Backspace` | **可能** | デフォルト: `行頭まで削除` | カーソル位置から行頭まで一括削除 |
| `Ctrl-a` | 不可 | システム予約 | 全選択 |
| `Ctrl-b` | 不可 | SKK標準予約 | 左移動 / 前候補 |
| `Ctrl-c` | 不可 | システム予約 | コピー |
| `Ctrl-d` | **可能** | デフォルト: `カーソル右削除` | カーソル右側の1文字削除 (Forward Delete) |
| `Ctrl-e` | **可能** | デフォルト: `行末移動` | 行末移動 (Line End) |
| `Ctrl-f` | 不可 | SKK標準予約 | 右移動 / 次候補 |
| `Ctrl-g` | 不可 | SKK標準予約 | キャンセル |
| `Ctrl-h` | **可能** | デフォルト: `1文字削除` | カーソル直前の1文字削除 (Backspace 相当) |
| `Ctrl-i` | **可能** | デフォルト: `補完` | 補完候補の提示・選択 |
| `Ctrl-j` | 不可 | SKK標準予約 | 確定 / かなキー |
| `Ctrl-k` | **可能** | デフォルト: `行末まで削除` | カーソル位置から行末まで削除 (Kill Line) |
| `Ctrl-l` | **可能** | デフォルト: `カーソル前のひらがなを変換` | カーソル直前のひらがなを見出し語として変換 |
| `Ctrl-m` | **可能** | デフォルト: `なし (割り当てなし)` | 未割り当て |
| `Ctrl-n` | 不可 | SKK標準予約 | 下移動 / 次候補 |
| `Ctrl-o` | **可能** | デフォルト: `なし (割り当てなし)` | 未割り当て |
| `Ctrl-p` | 不可 | SKK標準予約 | 上移動 / 前候補 |
| `Ctrl-q` | 不可 | SKK標準予約 | かな / カナ切替 |
| `Ctrl-r` | **可能** | デフォルト: `カーソル後のひらがなを変換` | カーソル直後のひらがなを見出し語として変換 |
| `Ctrl-s` | **可能** | デフォルト: `設定画面を開く` | アプリの設定画面を起動 |
| `Ctrl-t` | **可能** | デフォルト: `なし (割り当てなし)` | 未割り当て |
| `Ctrl-u` | **可能** | デフォルト: `再変換` | 再変換 (確定取り消し) |
| `Ctrl-v` | 不可 | システム予約 | 貼り付け |
| `Ctrl-w` | **可能** | デフォルト: `単語単位で削除` | カーソル左の語句を削除 |
| `Ctrl-x` | 不可 | システム予約 | 切り取り |
| `Ctrl-y` | 不可 | システム予約 | やり直し |
| `Ctrl-z` | 不可 | システム予約 | 元に戻す |

### 6. 設定オプション

#### 入力設定
- **入力形式による自動モード切替**: URI やパスワード入力欄などで自動的に英字モードへ切り替えます。
- **句点に「。」を使用する**: 「.」入力時に「。」(ON) または「．」(OFF) を入力します。
- **読点に「、」を使用する**: 「,」入力時に「、」(ON) または「，」(OFF) を入力します。
- **半角カタカナを使用する**: カナトグル（`q`）の切り替え順序に半角カタカナを含めます。
- **SandS (Space and Shift)**: スペースキーを単独で押すとスペース、他のキーと同時に押すと Shift キーとして機能させます。
- **Ctrlキーショートカットの設定**: Ctrl キーと文字キーの組み合わせ機能（カーソル移動、ひらがな変換、設定画面起動など）をカスタマイズします。

#### 表示設定
- **▽▼モード表示**: 入力中に ▽ や ▼ などの状態インジケータを表示します。
- **ツールチップ表示時間**: モード変更時の通知表示時間を調整します（0〜5秒、0で非表示）。
- **ツールチップ表示位置**: ツールチップの表示位置を「上」「下」から選択します。

#### 物理キーボード
- **日本語配列キーボード**: JIS配列キーボードを接続する場合に正しい文字が入力されるようにします。
- **入力モード切替**: 「半角/全角」、「英数」、「かな」キーで英語モード、日本語モード、トグル切り替えをおこなえるようにします。

#### 画面キーボード
- **キー操作時のバイブレーション**: 画面キーボードタップ時の触覚フィードバックを有効にします。
- **候補表示のスタイル**: 変換候補・単語登録画面の表示形式を「フローティング表示」（キャレット近辺にポップアップ表示）、「キーボード内表示 (従来の候補バー)」から選択します。
- **キーボードの種類**: 「画面キーボードなし」「記号バー」「QWERTY」「Tablet」「ストローク」から選択します。
- **キーボードの高さ**: 極小 (80%) 〜 極大 (120%) から画面キーボードのサイズを調整します。
- **キーボードの幅**: 画面キーボードのサイズの横幅（100%〜50%）を調整します。
- **キーボードの寄せ方向**: 画面キーボードのの配置を「左寄せ」「右寄せ」から選択します。
- **記号バーのカスタマイズ**: 記号バーのキー配列をドラッグ＆ドロップでカスタマイズします。
- **QWERTY配列のカスタマイズ**: QWERTY キーボードのキー配列をドラッグ＆ドロップでカスタマイズします。
- **Tablet配列のカスタマイズ**: Tablet キーボードのキー配列をドラッグ＆ドロップでカスタマイズします。

#### 辞書・学習
- **変換学習を有効にする**: 変換確定時の単語学習（順位の記憶や新規登録）を行います。
- **候補の最後で辞書登録する**: 候補リストの末尾を超えた際に自動で単語登録モードへ移行します。OFF の場合は最初の候補に戻ります。
- **ユーザー学習辞書ツール**: 学習された単語の一覧表示・個別削除・エクスポート・インポートを行います。
- **外部SKK辞書ツール**: `SKK-JISYO.L` 等の外部辞書の Web ダウンロード・インポート・クリアを行います。

#### バックアップ・復元
- **設定のバックアップ**: 現在の設定およびカスタマイズしたキー配列等を JSON ファイルに保存します。
- **設定の復元**: バックアップファイルから設定を復元します。
- **設定の初期化**: すべての設定を初期状態に戻します（※学習辞書は保持されます）。

#### アプリ情報
- **バージョン**: 現在のアプリバージョンを表示します。
- **法的情報・ライセンス**: ライセンス表記、商標免責事項、および免責事項を表示します。

### 7. 絵文字キーボード

絵文字キーボード機能を利用して、様々な絵文字（顔文字・動物・食べ物・乗り物・オブジェクト・記号・国旗など）を素早く入力できます。

- **絵文字パネルの表示**: 画面キーボードの絵文字ボタン（またはメニュー）から絵文字キーボードを開きます。
- **カテゴリ切り替え**: 上部（または下部）のタブから絵文字のカテゴリを選択できます。
- **入力**: 絵文字をタップすると、テキスト欄に直接入力されます。

### 8. Ctrl ショートカットのカスタマイズ

設定の「Ctrlキーショートカットの設定」から、物理キーボード等の `Ctrl` キーと組み合わせるショートカットキーの割り当てをカスタマイズできます。

- **割り当て可能なアクション**: カーソル移動、行頭/行末移動、削除、ひらがな変換、設定画面呼び出しなどを任意のキーに再割り当て可能です。

### 9. フローティング候補ウィンドウとチップ選択

変換候補の表示形式としてフローティング表示をサポートしています。

- **フローティング候補ウィンドウ**: キャレット（カーソル）の付近にポップアップで候補一覧を表示します。
- **2次元ナビゲーション**: フローティング表示中や候補選択中に `↑` `↓` `←` `→` や `Ctrl-p` / `Ctrl-n` / `Ctrl-b` / `Ctrl-f` を使って上下左右の候補を直感的に選択・自動スクロールできます。

### 10. ダークモード対応

Android のシステム設定に連動して、ダークテーマに対応したUIデザインに自動で切り替えます。
ダークモード固定、ライトモード固定にすることもできます

### 11. ローマ字かな変換表

詳細なローマ字かな変換ルールについては、[ROMAJI.md](ROMAJI.md) を参照してください。

### 12. ソフトウェアキーボード

物理キーボードの有無や好みに合わせて、画面上のキーボード表示を切り替えられます。

- **表示の切り替え**: 設定の「キーボードの種類」から「記号バー」「QWERTY」「タブレット」「ストローク」を選択してください。
  - 物理キーボードがない場合はデフォルトで **QWERTY** が選択されます。
  - 物理キーボードがある場合は、1行の記号入力補助である **記号バー** がデフォルト設定されます。
  - **タブレット**: 数字キーや矢印キー、Tab、Ctrl 等を備えた、PC 用キーボードに近い 5 段配列のレイアウトです。大画面のデバイスでの使用に適しています。
- **QWERTY / タブレット / 記号バーの操作**:
  - `Shift`: 大文字入力（SKK の読み開始など）に使用します。**2回連続で押すとロック状態**になり、解除するにはもう一度押してください。
  - `Ctrl`: `Ctrl-j` や `Ctrl-a` などの入力に使用します。一度押すと「ON」状態になり、次のキー入力に Ctrl が適用されます。
  - `@!?`: 画面キーボードで記号入力レイアウトに切り替えます。**2回連続で押すとロック状態**になり、記号入力を継続できます。解除するにはもう一度押すか、他のモードキー（Shift 等）を押してください。
  - `abc`: QWERTYの記号画面から通常画面（アルファベット画面）に戻ります。
- **カスタマイズ**:
  - 設定画面から「記号バー」「QWERTY配列」「Tablet配列」のキー配置をドラッグ＆ドロップ操作で自由に編集してください。
  - 各キーのウェイト（幅）や、特定の機能を割り当てた「機能キー」の配置も GUI 上で調整できます。
- **ストローク入力**:
  - 画面をなぞって文字を入力する Graffiti 風の手書き入力方式です。画面左側がアルファベット、右側が数字に対応しています。
  - 寄せ方向（左右）や入力エリアの幅（70%〜100%）を設定画面から調整してください。
  - 画面から上の外側へのストロークでヘルプ画面を表示できます。ヘルプ画面をタップでページ切り替え、ヘルプ画面外をタップで入力エリアに戻ります。

### 9. ストロークガイド

- アルファベット

<div style="background-color: #FFFFFF; display: inline-block; padding: 10px;">
  <img src="app/src/main/res/drawable/stroke_help1.png" width="400" alt="Alphabet Help">
</div>

- 数字と機能

<div style="background-color: #FFFFFF; display: inline-block; padding: 10px;">
  <img src="app/src/main/res/drawable/stroke_help2.png" width="400" alt="Number Help">
</div>

- 記号

<div style="background-color: #FFFFFF; display: inline-block; padding: 10px;">
  <img src="app/src/main/res/drawable/stroke_help3.png" width="400" alt="Punctuation Help">
</div>
